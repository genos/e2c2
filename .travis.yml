language: cpp
sudo: required
compiler: clang
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y libgmp-dev libntl-dev
script:
  - cd src/
  - make -j
  - make test
  - make clean
notifications:
  email: false
